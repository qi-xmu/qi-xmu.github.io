<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://qi-xmu.github.io</id>
    <title>Gridea</title>
    <updated>2021-03-03T11:35:59.847Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://qi-xmu.github.io"/>
    <link rel="self" href="https://qi-xmu.github.io/atom.xml"/>
    <subtitle>温故而知新</subtitle>
    <logo>https://qi-xmu.github.io/images/avatar.png</logo>
    <icon>https://qi-xmu.github.io/favicon.ico</icon>
    <rights>All rights reserved 2021, Gridea</rights>
    <entry>
        <title type="html"><![CDATA[C++中map， vector， pair]]></title>
        <id>https://qi-xmu.github.io/post/czhong-map-vector-pair/</id>
        <link href="https://qi-xmu.github.io/post/czhong-map-vector-pair/">
        </link>
        <updated>2021-03-03T11:18:53.000Z</updated>
        <summary type="html"><![CDATA[<h2 id="c中map的用法">C++中map的用法</h2>
<h4 id="map的种类">map的种类</h4>
<figure data-type="image" tabindex="1"><img src="https://qi-xmu.github.io/post-images/1614770509108.png" alt="" loading="lazy"></figure>
<h4 id="需要的头文件">需要的头文件</h4>
<pre><code class="language-cpp">#include &lt;map&gt;
// #include &lt;multimap&gt;	
#include &lt;unordered_map&gt;
</code></pre>
<h4 id="定义方法">定义方法</h4>
]]></summary>
        <content type="html"><![CDATA[<h2 id="c中map的用法">C++中map的用法</h2>
<h4 id="map的种类">map的种类</h4>
<figure data-type="image" tabindex="1"><img src="https://qi-xmu.github.io/post-images/1614770509108.png" alt="" loading="lazy"></figure>
<h4 id="需要的头文件">需要的头文件</h4>
<pre><code class="language-cpp">#include &lt;map&gt;
// #include &lt;multimap&gt;	
#include &lt;unordered_map&gt;
</code></pre>
<h4 id="定义方法">定义方法</h4>
<!-- more -->
<pre><code class="language-cpp">std::map&lt;var_type&gt; var_name;
// 和pair一起使用 
std::map&lt;var1_type, var2_type&gt; var_name;
var_name.insert(pair&lt;var1_type, var2_type&gt; (var1_value, var2_value));
</code></pre>
<h4 id="常用成员方法">常用成员方法</h4>
<table>
<thead>
<tr>
<th>方法</th>
<th>功能</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>begin()</code></td>
<td>返回指向容器中第一个（注意，是已排好序的第一个）键值对的双向迭代器。如果 multimap 容器用 const 限定，则该方法返回的是 const 类型的双向迭代器。</td>
</tr>
<tr>
<td><code>end()</code></td>
<td>返回指向容器最后一个元素（注意，是已排好序的最后一个）所在位置后一个位置的双向迭代器，通常和 begin() 结合使用。如果 multimap 容器用 const 限定，则该方法返回的是 const 类型的双向迭代器。</td>
</tr>
<tr>
<td><code>empty()</code></td>
<td>若容器为空，则返回 true；否则 false。</td>
</tr>
<tr>
<td><code>find(key)</code></td>
<td>在 multimap 容器中查找首个键为 key 的键值对，如果成功找到，则返回指向该键值对的双向迭代器；反之，则返回和 end() 方法一样的迭代器。另外，如果 multimap 容器用 const 限定，则该方法返回的是 const 类型的双向迭代器。</td>
</tr>
<tr>
<td><code>size()</code></td>
<td>返回当前 multimap 容器中存有键值对的个数。</td>
</tr>
<tr>
<td><code>insert()</code></td>
<td>向 multimap 容器中插入键值对。</td>
</tr>
<tr>
<td><code>erase()</code></td>
<td>删除 multimap 容器指定位置、指定键（key）值或者指定区域内的键值对。</td>
</tr>
<tr>
<td><code>swap()</code></td>
<td>交换 2 个 multimap 容器中存储的键值对，这意味着，操作的 2 个键值对的类型必须相同。</td>
</tr>
<tr>
<td><code>clear()</code></td>
<td>清空 multimap 容器中所有的键值对，使 multimap 容器的 size() 为 0。</td>
</tr>
<tr>
<td><code>count(key)</code></td>
<td>在当前 multimap 容器中，查找键为 key 的键值对的个数并返回。</td>
</tr>
<tr>
<td><code>max_size()</code></td>
<td>返回 multimap 容器所能容纳键值对的最大个数，不同的操作系统，其返回值亦不相同。</td>
</tr>
</tbody>
</table>
<h2 id="c中vector">C++中vector</h2>
<p><strong>vector</strong>：向量（Vector）是一个封装了动态大小数组的顺序容器（Sequence Container）。跟任意其它类型容器一样，它能够存放各种类型的对象。可以简单的认为，向量是一个能够存放任意类型的动态数组。</p>
<h4 id="特点">特点</h4>
<ul>
<li>顺序序列</li>
<li>动态数组</li>
<li>能够感知内存分配器的（Allocator-aware）</li>
</ul>
<h4 id="定义方法-2">定义方法</h4>
<pre><code class="language-cpp">// 一维向量
vector&lt;var_type&gt; var_name;
// 二维向量
vector&lt;vector&lt;var_type&gt; &gt; var_name;
</code></pre>
<h4 id="基本用法">基本用法</h4>
<table>
<thead>
<tr>
<th>方法</th>
<th>功能</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>push_back()</code></td>
<td>在数组的最后添加一个数据。</td>
</tr>
<tr>
<td><code>pop_back()</code></td>
<td>去掉数组的最后一个数据。</td>
</tr>
<tr>
<td><code>at()</code></td>
<td>得到编号位置的数据。功能同。<code>num[i]</code></td>
</tr>
<tr>
<td><code>begin()</code></td>
<td>得到数组头的指针。</td>
</tr>
<tr>
<td><code>end()</code></td>
<td>得到数组的最后一个单元+1的指针。</td>
</tr>
<tr>
<td><code>front()</code></td>
<td>返回数组头的值。</td>
</tr>
<tr>
<td><code>back()</code></td>
<td>返回数组最后一个元素的值。</td>
</tr>
<tr>
<td><code>size()</code></td>
<td>当前使用数据的大小。</td>
</tr>
<tr>
<td><code>erase()</code></td>
<td>删除指针指向的数据项。</td>
</tr>
<tr>
<td><code>empty()</code></td>
<td>判断向量是否为空。</td>
</tr>
<tr>
<td><code>swap(vector)</code></td>
<td>与另一个vector交换数据。</td>
</tr>
</tbody>
</table>
<h4 id="向量的排序">向量的排序</h4>
<pre><code class="language-cpp">#include &lt;algorithm&gt;
// 从小到大
sort(vec.begin(), vec.end());
// 反转
reverse(obj.begin(),obj.end());
</code></pre>
<h4 id="向量的输出">向量的输出</h4>
<pre><code class="language-cpp">//声明一个迭代器，来访问vector容器，作用：遍历或者指向vector容器的元素。
vector&lt;int&gt;::iterator it;
for(it = obj.begin(); it != obj.end(); it++)
    cout &lt;&lt; *it &lt;&lt; &quot; &quot;;
cout &lt;&lt; endl;
</code></pre>
<h2 id="c中的pair">C++中的pair</h2>
<p><strong>pair</strong>：将2个数据组合成一组数据，当需要这样的需求时就可以使用pair，如stl中的map就是将key和value放在一起来保存。另一个应用是，当一个函数需要返回2个数据的时候，可以选择pair。 pair的实现是一个结构体，主要的两个成员变量是first second 因为是使用struct不是class，所以可以直接使用pair的成员变量。</p>
<h4 id="定义">定义</h4>
<pre><code class="language-cpp">pair&lt;var1_type, var2_type&gt; var_name(var1_value, var2_value);

var_name = make_pair(var1_value, var2_value)
</code></pre>
<h4 id="访问">访问</h4>
<pre><code>pair&lt;int, int&gt; point;
point.first;
point.second;
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Hello Gridea]]></title>
        <id>https://qi-xmu.github.io/post/hello-gridea/</id>
        <link href="https://qi-xmu.github.io/post/hello-gridea/">
        </link>
        <updated>2018-12-11T16:00:00.000Z</updated>
        <summary type="html"><![CDATA[<p>👏  欢迎使用 <strong>Gridea</strong> ！<br>
✍️  <strong>Gridea</strong> 一个静态博客写作客户端。你可以用它来记录你的生活、心情、知识、笔记、创意... ...</p>
]]></summary>
        <content type="html"><![CDATA[<p>👏  欢迎使用 <strong>Gridea</strong> ！<br>
✍️  <strong>Gridea</strong> 一个静态博客写作客户端。你可以用它来记录你的生活、心情、知识、笔记、创意... ...</p>
<!-- more -->
<p><a href="https://github.com/getgridea/gridea">Github</a><br>
<a href="https://gridea.dev/">Gridea 主页</a><br>
<a href="http://fehey.com/">示例网站</a></p>
<h2 id="特性">特性👇</h2>
<p>📝  你可以使用最酷的 <strong>Markdown</strong> 语法，进行快速创作</p>
<p>🌉  你可以给文章配上精美的封面图和在文章任意位置插入图片</p>
<p>🏷️  你可以对文章进行标签分组</p>
<p>📋  你可以自定义菜单，甚至可以创建外部链接菜单</p>
<p>💻  你可以在 <strong>Windows</strong>，<strong>MacOS</strong> 或 <strong>Linux</strong> 设备上使用此客户端</p>
<p>🌎  你可以使用 <strong>𝖦𝗂𝗍𝗁𝗎𝖻 𝖯𝖺𝗀𝖾𝗌</strong> 或 <strong>Coding Pages</strong> 向世界展示，未来将支持更多平台</p>
<p>💬  你可以进行简单的配置，接入 <a href="https://github.com/gitalk/gitalk">Gitalk</a> 或 <a href="https://github.com/SukkaW/DisqusJS">DisqusJS</a> 评论系统</p>
<p>🇬🇧  你可以使用<strong>中文简体</strong>或<strong>英语</strong></p>
<p>🌁  你可以任意使用应用内默认主题或任意第三方主题，强大的主题自定义能力</p>
<p>🖥  你可以自定义源文件夹，利用 OneDrive、百度网盘、iCloud、Dropbox 等进行多设备同步</p>
<p>🌱 当然 <strong>Gridea</strong> 还很年轻，有很多不足，但请相信，它会不停向前 🏃</p>
<p>未来，它一定会成为你离不开的伙伴</p>
<p>尽情发挥你的才华吧！</p>
<p>😘 Enjoy~</p>
]]></content>
    </entry>
</feed>